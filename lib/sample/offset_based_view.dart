import 'package:flutter/material.dart';

/// Offset based Pagingのサンプル
class OffsetBasedView extends StatelessWidget {
  const OffsetBasedView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('OffsetBasedView'),
    );
  }
}
